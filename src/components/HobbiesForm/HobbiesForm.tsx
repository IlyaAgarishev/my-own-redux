import Store from "../../myOwnRedux/store";

const HobbiesForm = () => {
  return (
    <span>
      <input type="text" placeholder="Hobby" />
      <button>Add</button>
    </span>
  );
};

export default HobbiesForm;
